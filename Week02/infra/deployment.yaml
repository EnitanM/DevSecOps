apiVersion: apps/v1
kind: Deployment
metadata:
  name: personalproject
  labels:
    app: flask-app-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app-test
  template:
    metadata:
      labels:
        app: flask-app-test
    spec:
      containers:
      - name: flask-app-test
        image: capstoneteam1-avd2dagya7gjdhda.azurecr.io/personalproject:latest
      imagePullSecrets:
      - name: acr-auth
        ports:
        - containerPort: 8081